<%- include('../partials/header') %>

<table id="list">
  <thead>
    <tr>
      <th>🍄Title</th>
      <th>🍄Release<br>Year</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% anime.forEach(function(a) { %>
      <tr>
        <td><%= a.title %></td>
        <td><%= a.releaseYear %></td>
        <td><a href="/anime/<%= a._id %>">DETAILS</a></td>
      </tr>
      <% }); %>
    </tbody>
  </table>
  
<%- include('../partials/footer') %>
